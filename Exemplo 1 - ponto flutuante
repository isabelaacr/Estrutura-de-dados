// Converter Celsius para Fahrenheit

#include <stdio.h>

int main ( )
{
float c;
float f; //armazeno os valores
scanf ("f", &c); //captura valor fornecido
f = 1.8 * c + 32; //conversão
printf ("&f"\n", f); //exibe resultado
return 0;
}

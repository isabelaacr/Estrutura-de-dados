#include <stdio.h>

int main () {
    int N,x,i,v[100];
while (x >= 0) {
  v[N] = x;
  N++;
scanf ("%d", &x);
}

printf ("O vetor possui %d elementos\n", N).
  for (i= N-1; i >= 0; i++) {
    printf ("%d\n", v[i-1])
  }

return 0;
}
